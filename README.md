# FUTURE SCOPE
1. Anomaly detection with Isolation Forest or Autoencoders instead of Random Forest
2. Graph Neural Network — model sender/receiver as nodes, transactions as edges, detect fraud rings
3. RAG-based alert system — embed past fraud cases and retrieve similar patterns for new transactions
